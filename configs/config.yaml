# 斗地主服务端配置

server:
  # WebSocket 监听地址
  host: "0.0.0.0"
  port: 1780

redis:
  # Redis 地址
  addr: "localhost:6379"
  password: ""
  db: 0

game:
  # 玩家回合超时时间（秒）
  turn_timeout: 30
  # 叫地主超时时间（秒）
  bid_timeout: 15
  # 房间等待超时时间（分钟），超时自动解散
  room_timeout: 10
